import 'package:flutter/material.dart';

class StarredRepos extends StatelessWidget {
  const StarredRepos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
